<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_sort_method_titles"
        android:entryValues="@array/pref_sort_method_values"
        android:key="sort_method"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_sort_method" />

    <ListPreference
        android:defaultValue="10"
        android:entries="@array/pref_recommendations_size_titles"
        android:entryValues="@array/pref_recommendations_size_values"
        android:key="recommendations_size"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_recommendations_size" />
</PreferenceScreen>
